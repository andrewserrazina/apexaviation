.claude/
node_modules/
dist/
.env
.env.local
.env.*.local

# design-sync (see /design-sync skill)
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/

# test/run_security_regression_tests.sh compiles validateAcsTaskId.ts to
# plain JS here so it can be unit-tested under Node (v119 Rev2) -- never
# a tracked artifact.
test/__v119_compiled/
